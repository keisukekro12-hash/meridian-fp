# FP Knowledge Base — 累積学習メモリ

> **このファイルの使い方**
> - セッション開始時に必ず読み込み、全セクションを作業に反映する
> - 「お疲れ様」のたびに、このセッションで学んだことを各セクションに追記する
> - 古くなった情報は打ち消し線（~~テキスト~~）で残しつつ更新する
> - トークン節約のため、同じ内容の重複追記はしない

最終更新: 2026-09-12

---

## 🚨 作業前チェックリスト（毎回確認）

### Git / GitHub
- [ ] `git status` でブランチの状態を確認（behind なら `git pull --rebase` してから作業）
- [ ] push は SSH 経由のみ（HTTPS は認証エラーになる。`git remote -v` で `git@github.com:` 始まりか確認）
- [ ] CLAUDE.md を変更したら必ずコミット＆プッシュ（しないとリモートセッションに届かない）

### 投稿文・コンテンツ生成
- [ ] 商品の使い方・設置場所を「推測」で書かない。商品説明に明記されているか必ず確認
- [ ] 「〇〇に取り付ける」「〇〇に使える」という表現が入るときは根拠を確認してから書く
- [ ] 不確かな場合は寸法・素材など確実な事実だけで訴求する

### 外部ツール・API
- [ ] macOS でシェルスクリプトから JSON を処理するなら `python3` を使う（`jq` は未インストールの場合が多い）
- [ ] OpenAI 画像生成: `gpt-image-1` を使う（DALL-E 3 は 2026年5月廃止済み）
- [ ] gpt-image-1 は 2026年10月23日廃止予定 → 廃止前に `gpt-image-1.5` へ移行

---

## 🔄 ミスパターン → 修正後の正解

### [2026-05-13] jq 未インストールによるスクリプト失敗
- **ミス**: シェルスクリプトで `jq` を使って JSON を処理しようとした
- **原因**: macOS デフォルトには `jq` が入っていない
- **正解**: `/usr/bin/python3` で JSON を処理する（常駐している）

### [2026-09-07] 商品の使い方誤読による誤情報（タオルハンガー事件）
- **ミス**: 山崎実業のシンク下タオルハンガーを「扉の内側に取り付ける」と誤って生成
- **原因**: 商品名に「扉」という文字があったため、推測で書いてしまった
- **正解**: 商品説明に明記されている使い方だけを書く。不確かなら「サイズ・素材」など事実だけで訴求

### [2026-09-08] GitHub HTTPS push 失敗
- **ミス**: `git push` が認証エラー（`Invalid username or token`）
- **原因**: GitHub は 2021年以降パスワード認証を廃止。HTTPS では PAT か SSH が必要
- **正解**: SSH キーを設定し、リモートURLを SSH に切り替える
  ```bash
  git remote set-url origin git@github.com:<user>/<repo>.git
  ```

### [2026-09-08] ブランチが origin より遅れている状態での push
- **ミス**: コミット後すぐ `git push` しようとして rejected
- **原因**: ブランチが origin/main より複数コミット遅れていた
- **正解**: `git stash → git pull --rebase → git stash pop → git push` の順で解決

### [2026-09-08] CLAUDE.md 変更がリモートセッションに届かない
- **ミス**: CLAUDE.md を編集したのに、リモートセッションの Claude に反映されなかった
- **原因**: ローカルの変更をコミット＆プッシュしていなかった
- **正解**: 変更後は即 `git add CLAUDE.md && git commit && git push`

### [2026-09-10] DALL-E 3 API が廃止済み
- **ミス**: DALL-E 3 で画像生成しようとした
- **原因**: 知識が古かった
- **正解**: `gpt-image-1`（または `gpt-image-1.5`）を使う

---

## 🎨 成果物スタイル・好み

### Threads 投稿
- **AI 臭さ NG**: 「〜です！」「〜しましょう！」という機械的な文体は避ける
- **インテリア画像の 3 トーン原則**: 明（壁・床 60%）/ 中（ソファ）/ 暗（テーブル 30%）
- 単一トーンで全家具を揃えない（全家具同色は NG）
- 参考設計式: 白・グレー系壁 × ウッド系床 × ダーク系アクセント家具

### 一般
- （今後追記）

---

## 💡 思考プロセス・提案スタイル

### リサーチ方針
- 複数ソース比較・批判的フィルタリングを必ず行う
- 単一ソースを信用しない。複数ソースの一致点のみを採用
- 優先順位: バズ実績（実績あり）> kei 承認履歴（センス）> Webリサーチ（外部）

### 提案スタイル
- 選択肢を出すときは「採用理由」と「却下理由」をセットで示す
- リスクや残課題も必ず添える
- （今後追記）

---

## 📚 プロジェクト固有ノウハウ

### システム構成
- **フック 2 層構造**:
  - グローバル（`~/.claude/settings.json` → `~/.claude/scripts/end-of-day.sh`）: 全セッション共通アーカイブ
  - FP プロジェクト（`.claude/settings.local.json` → `.claude/hooks/session-wrap.sh`）: プロジェクト固有ログ
- **リモートセッション対応**: CLAUDE.md のルールで Claude 本体が対応（フックはリモート環境で使えない）

### ファイル・ディレクトリ構成
- `議事録/` — 全セッションの記録（アーカイブ）
- `意思決定ログ/` — 決定事項がある場合のみ
- `FP_knowledge.md` — 本ファイル（生きた知識メモリ・毎回ロード）
- `タスク管理/教訓.md` — ミスパターン詳細（FP_knowledge.md の補足）
- `threads/generate.py` — Threads 投稿文・画像自動生成スクリプト
- `.env` — API キー保存（gitignore 済み）

### Threads 自動化パイプライン
```
Webリサーチ → Claude がスタイル判断（3トーン原則強制）
→ 投稿文生成 → 画像生成（gpt-image-1）→ 履歴保存（history.json）
--approve で approved.json に蓄積 / --viral で viral.json に蓄積
```

### OpenAI API
- モデル: `gpt-image-1`（2026年10月廃止予定 → `gpt-image-1.5` へ移行）
- コスト: 1枚あたり約 6〜10 円
- 残高管理: platform.openai.com で確認、Auto-reload は OFF 推奨

---

## 📝 更新ログ

| 日付 | 更新内容 |
|---|---|
| 2026-09-12 | 初版作成。過去全議事録・意思決定ログ・教訓.md から蒸留 |
